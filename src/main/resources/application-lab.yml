flashflow:
  admission:
    mode: ${FLASHFLOW_ADMISSION_MODE:MYSQL_ONLY}
  expiration:
    scheduling-enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

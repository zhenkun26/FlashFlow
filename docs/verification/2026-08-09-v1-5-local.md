# V1.5 controlled local verification — 2026-08-09

This report records one controlled local experiment matrix. It is correctness and characterization evidence for the tested workspace, not a production QPS, availability, or capacity claim.

## Attribution and environment

- Git revision: `47923907fc781b86a4e457280468b226c4a5f56f`.
- Worktree: dirty for all runs because the V1.5 implementation was under verification; every raw report records this warning, so results are not attributable to the revision alone.
- Host: Apple arm64, macOS 26.5 (`Darwin 25.5.0`).
- Runtime: OpenJDK 26.0.2 compiling with Java 21 release target; Maven 3.9.16.
- Database: MySQL 8.4.6 on Docker Desktop 29.4.1, exposed on host port 3307.
- Load generator: k6 2.1.0 on the same host as the application and database.
- Window for the selected matrix: 2026-08-09 06:19:31Z through 06:20:59Z.

## Commands and correctness gate

```bash
openspec validate build-v1-5-concurrency-experiment-matrix --strict
mvn -o -Dmaven.repo.local=/private/tmp/flashflow-m2 test
FLASHFLOW_MYSQL_PORT=3307 CAPTURE_MYSQL_DIAGNOSTICS=true ./scripts/run-experiment-matrix.sh
```

- OpenSpec strict validation: `PASS`.
- Complete Maven suite: `PASS` — 31 tests, 0 failures, 0 errors, 0 skipped.
- Matrix completeness guard: `PASS` — 7 planned cases and 7 executed cases.
- Each characterization references the same successful correctness-gate log.

## Controlled inputs

The canonical baseline used `CONDITIONAL_ATOMIC`, 10 VUs, 5 seconds, stock 100, one hot SKU, Hikari pool size 10, 3,000 ms connection timeout, optimistic retry budget 3, and whole-transaction retry budget 3. Each variant changed only its declared factor:

| Case | Declared change from baseline |
|---|---|
| `strategy-pessimistic` | strategy to `PESSIMISTIC` |
| `vus-low` | VUs from 10 to 5 |
| `pool-small` | pool size from 10 to 5 |
| `retry-zero` | optimistic and transaction retry budgets from 3 to 0 |
| `stock-low` | total stock from 100 to 20 |
| `uniform-four-skus` | contention shape from one hot SKU to uniform across four SKUs |

The checked-in validator rejected unknown, unsafe-profile, missing, and multi-factor comparisons before execution.

## Classified results

All seven reports were `PASS`: request totals reconciled with classified outcomes, unexpected responses and Hikari acquisition timeouts were zero, and required evidence collection completed.

| Case | Requests | Created | Sold out | Retryable contention | Unexpected | p95 ms | Transaction retries | Retry exhausted |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `baseline` | 1,794 | 100 | 710 | 984 | 0 | 42.49 | 4,892 | 984 |
| `strategy-pessimistic` | 1,944 | 100 | 762 | 1,082 | 0 | 39.29 | 5,355 | 1,082 |
| `vus-low` | 1,957 | 100 | 1,279 | 578 | 0 | 23.72 | 3,500 | 578 |
| `pool-small` | 2,075 | 100 | 1,557 | 418 | 0 | 52.91 | 2,879 | 418 |
| `retry-zero` | 5,180 | 100 | 658 | 4,422 | 0 | 15.95 | 4,422 | 4,422 |
| `stock-low` | 1,889 | 20 | 802 | 1,067 | 0 | 39.57 | 5,278 | 1,067 |
| `uniform-four-skus` | 3,224 | 100 | 2,920 | 204 | 0 | 33.05 | 1,938 | 204 |

`ACTIVITY_NOT_ACTIVE`, `EXISTING_EFFECTIVE_ORDER`, and `IDEMPOTENCY_CONFLICT` were zero because this workload used one active activity and distinct user/idempotency identities. Retryable 503 responses are reported separately from business rejection and arose after bounded whole-transaction retries; they are not successful creates.

The sequential closed-loop cases ran on one developer machine. Differences in total requests and latency may include host scheduling and warm-state noise, so the table supports local behavioral comparison only. It does not rank universal strategy performance.

## Committed-state evidence

Every case ended with:

- stock conservation and no negative balance;
- effective orders = purchase claims = reserved reservations = inventory movements;
- no effective order without a claim, claim without an effective order, reservation mismatch, duplicate movement operation, or effective-order count above initial stock.

The six 100-stock cases each committed exactly 100 effective orders, claims, reservations, and movements with final balance `100 = 0 + 100 + 0`. The 20-stock case committed exactly 20 of each with `20 = 0 + 20 + 0`.

## Diagnostic evidence

A final baseline run (`20260809T062219Z-baseline-b11c73d0-293f-4653-b640-6ea9f0606fdf`) verified optional diagnostic capture. It retained timestamped InnoDB row-lock counters and `SHOW ENGINE INNODB STATUS`, including a latest detected deadlock, while required outcome and invariant evidence still passed. The row-lock counters are server-global cumulative values and are not presented as per-run deltas.

Selected matrix run identifiers:

- `20260809T061931Z-baseline-119992d9-b14d-4487-beb3-50e213b6402e`
- `20260809T061944Z-strategy-pessimistic-ada70785-22c0-4adc-b497-7e40f6a40893`
- `20260809T061957Z-vus-low-e11bd6f1-6846-4dc8-8bd8-204048502cb6`
- `20260809T062010Z-pool-small-5d9aa5bb-bce0-43c3-b01a-f14c918d9f8b`
- `20260809T062023Z-retry-zero-03cec153-9d3b-4e92-9745-dc6eb84ca65a`
- `20260809T062036Z-stock-low-37e115c8-327b-4200-b2e8-e83a7bf7be22`
- `20260809T062049Z-uniform-four-skus-cd4d0878-a097-4fae-8ebb-072121a4e856`

Raw run directories are append-only local artifacts under `experiments/runs/` and are intentionally ignored by Git. The manifest, schema, orchestration, invariant query, reporter, and this curated report are checked-in artifacts.

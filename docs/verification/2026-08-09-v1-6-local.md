# V1.6 stock-first ordering local evidence — 2026-08-09

Status: **PASS**, with one earlier infrastructure attempt retained as **BLOCKED**.

## Scope and environment

This report characterizes one local Docker Desktop environment only: OpenJDK 26.0.2 compiling the Java 21 target, Docker 29.4.1, pinned MySQL 8.4.6, and Darwin 25.5.0 arm64. The worktree was dirty, so the runs are attributable to their captured manifests and artifacts rather than Git revision `4792390` alone.

Correctness commands:

```bash
mvn -o -Dmaven.repo.local=/private/tmp/flashflow-m2 test
openspec validate --all --strict
```

Results: 37 Maven tests passed with zero failures, errors, or skips; five OpenSpec items passed strict validation. The deterministic two-transaction fixture observed two `S,REC_NOT_GAP` locks on the stock primary key after child inserts, then exactly one recognized deadlock victim when both transactions requested the stock update. Its stock-first control committed both transactions and passed stock, order, claim, reservation, and movement invariants.

## Controlled comparison

Both cases used profile `lab`, conditional atomic reservation, 10 VUs, five seconds, one hot SKU, initial stock 100, pool size 10, 3000 ms connection timeout, optimistic retry budget 3, and database transaction retry budget 3. Only `TRANSACTION_SEQUENCE` changed.

```bash
DOCKER_HOST=unix:///Users/yuzheng/.docker/run/docker.sock \
FLASHFLOW_MYSQL_PORT=3308 \
./scripts/run-experiment.sh child-first-legacy-lab

DOCKER_HOST=unix:///Users/yuzheng/.docker/run/docker.sock \
FLASHFLOW_MYSQL_PORT=3308 \
CORRECTNESS_GATE_STATUS=PASS \
CORRECTNESS_GATE_REFERENCE=<old-run>/correctness-gate.log \
./scripts/run-experiment.sh stock-first-lab
```

| Evidence | Child first legacy | Stock first |
|---|---:|---:|
| Requests | 1,677 | 3,823 |
| Created | 100 | 100 |
| Sold out | 650 | 3,723 |
| Retryable contention | 927 | 0 |
| Transient database retries | 4,598 | 0 |
| Retry exhaustion | 927 | 0 |
| Claim-race rollback/replay/exhaustion | 0 / 0 / 0 | 0 / 0 / 0 |
| Unexpected responses/failures | 0 | 0 |
| Mean latency | 29.66 ms | 13.01 ms |
| p95 latency | 46.41 ms | 21.48 ms |
| Pool acquisition timeout | 0 | 0 |
| Final stock equation | `100 = 0 + 100 + 0` | `100 = 0 + 100 + 0` |
| Committed-state invariant violations | 0 | 0 |

The old sequence's transient retries match the deterministically proven FK shared-lock-to-stock-exclusive-lock upgrade graph. The stock-first sample recorded no transient database retry, retry exhaustion, claim-race metric, or unexpected failure. This supports removal of that specific cycle under this workload; it does not establish that all InnoDB deadlocks or retryable responses are impossible.

## Evidence attribution and boundary

- Child-first raw run: `20260809T071303Z-child-first-legacy-lab-a92f633f-d080-4d80-b665-15328461b71b`.
- Stock-first raw run: `20260809T071341Z-stock-first-lab-ecde5b0b-3dad-4ec3-b852-997066120fcc`.
- An earlier child-first attempt, `20260809T071224Z-child-first-legacy-lab-1cc81da3-577f-42f9-9910-fdfe491c4841`, is **BLOCKED** because the shell Docker context referenced a missing OrbStack socket. It was not treated as an application failure or silently removed.

Raw run directories are ignored local artifacts. This checked-in summary retains the exact identifiers and reconciled evidence. These local results are not production QPS, availability, or capacity claims.

# V2.1 local readiness report — 2026-08-09

Overall V3 readiness: **BLOCKED**.

The V2.1 implementation gates pass in the current worktree, but the worktree is not yet a committed attributable revision. Commit/push and OpenSpec archive are separately authorized actions, so this report does not promote the current evidence to a V3-ready claim.

## Identity and environment

- Clean V2 baseline: `02d224fd8e096edb5c0552152055818034229362`
- Evidence state: dirty V2.1 worktree based on that revision
- Java: OpenJDK 26.0.2 compiling Java 21
- Docker: Docker Desktop 29.4.1
- Database/cache: MySQL 8.4.6 and Redis 7.4.2-alpine through Testcontainers
- Broker spike: Apache RocketMQ 5.3.4, one NameServer, one Broker with integrated Proxy, `SYNC_FLUSH`, disposable tmpfs storage

## Gates

| Gate | Status | Evidence |
|---|---|---|
| Complete Maven/Testcontainers suite | PASS | 80 tests, 0 failures, 0 errors, 0 skipped |
| Explicit synchronous compatibility | PASS | 24 core tests selected under `FLASHFLOW_ADMISSION_MODE=MYSQL_ONLY`; 0 failures/errors/skips |
| Command contracts and interleavings | PASS | Version/fingerprint, ledger convergence, duplicate delivery, pre-commit interruption, post-commit acknowledgement loss, and sync/command race tests are included in the 80-test suite |
| Expiration trigger races | PASS | Early/duplicate/paid trigger, payment ordering, scanner recovery, and trigger/scanner cases passed |
| Redis fault and reconciliation compatibility | PASS | Existing outage, ambiguity, lifecycle, and reconciliation suites remained green in the full run |
| Experiment manifest | PASS | Schema-aware CLI validated 11 cases; the RocketMQ case resolves all pinned messaging inputs |
| RocketMQ topology and real synthetic round trip | PASS | `reports/messaging/20260809T092454Z-rocketmq-spike`: Broker V5_3_4 registered, send returned `SEND_OK`, consumer read the same versioned synthetic body, and Broker config reported `SYNC_FLUSH` |
| Synthetic messaging fault matrix | PASS | Duplicate/redelivery, poison/unsupported version, producer response loss, consumer interruptions, acknowledgement loss, restart classification, and bounded delay observation passed |
| Strict OpenSpec | PASS | 7/7 change and main-spec items passed `openspec validate --all --strict` |
| Diff whitespace and Compose resolution | PASS | `git diff --check` and final messaging-spike Compose resolution passed |
| Clean attributable V2.1 revision | BLOCKED | No V2.1 commit has been authorized or created |
| Commit / push / publication | NOT_RUN | Requires separate authorization |
| Delta sync / archive | NOT_RUN | Apply workflow leaves the completed change active for review; archive is a separate action |

## Reconciliation and claim limits

The deterministic harness rejects inconsistent produced, acknowledged, ambiguous, delivered, redelivered, consumed, rejected, unresolved, stable-command, and committed-effect totals. Message attempts are not reported as order throughput. The real Broker probe establishes only local compatibility with the pinned topology; it does not exercise application traffic or establish redelivery, failover, persistence, delay SLA, availability, capacity, or production readiness.

Two earlier topology-only reports were generated before the script verified Broker registration content, and one corrected attempt retained `FAIL` when a topic route was not yet visible. They remain append-only investigation evidence and are superseded by the final `092454Z` report; only that final report is used for the PASS row above.

After an authorized commit, rerun every required gate against the resulting clean revision before changing overall V3 readiness from `BLOCKED` to `PASS`.

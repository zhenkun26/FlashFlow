# V3 local live-messaging report — 2026-08-10

Overall implementation status: **PASS on the current dirty worktree**. Release-completion status: **NOT_RUN** because the required clean, attributable implementation revision has not yet been created and retested.

## Identity and environment

- Tested base revision recorded by the retained gate: `810f06e5e0a5570e1000049542d1d26bf7c40bce`
- Worktree during the retained gate: dirty (`dirtyWorktree=true`)
- Java: OpenJDK 26.0.2 compiling the Java 21 release target
- Docker: Docker Desktop 29.4.1
- Database/cache: MySQL 8.4.6 and Redis 7.4.2-alpine through Testcontainers
- Messaging: RocketMQ classic Java client 5.3.3, Broker 5.3.4, `SYNC_FLUSH`
- Final append-only report: `reports/messaging/20260810T121449Z-v3-live`

## Gates

| Gate | Status | Evidence |
|---|---|---|
| Complete Maven/Testcontainers suite | PASS | `mvn test`: 100 tests, 0 failures, 0 errors, 0 skipped |
| Real RocketMQ application/fault selection | PASS | 10 tests in the final `live-gate.log`; includes HTTP acceptance/status, broker outage/restart, poison DLQ, order acknowledgement loss, retry exhaustion, expiration acknowledgement loss, scanner recovery, V1 compatibility, and controlled comparison |
| Deterministic messaging/reconciliation selection | PASS | 18 tests in the final `deterministic-gate.log`; 0 failures/errors/skips |
| Broker acknowledgement contract | PASS | A live request returns `202` only after `SEND_OK`; a stopped Broker returns `503`/retryable and the same command identity completes after restart |
| At-least-once convergence | PASS | Real acknowledgement-loss tests redelivered the same stable identity while retaining one committed order or one unpaid closure |
| Retry exhaustion and DLQ | PASS | Real bounded reconsumption reached the dedicated application DLQ without a business effect or unsafe Redis release; manual replay retained the same command identity |
| Delayed expiration recovery | PASS | Real delayed delivery and acknowledgement loss converge through the locked MySQL boundary; a missing/late trigger is recovered by the scanner |
| Redis/MySQL reconciliation | PASS | Prepared, ambiguous, retryable, and dead-lettered commands are classified without treating Broker state as business truth |
| Controlled local comparison | PASS | Synchronous acceptance/completion `91.9/91.9 ms`; V3 acceptance/completion `27.3/82.9 ms`; one local request per path, explicitly `LOCAL_SINGLE_REQUEST_NOT_CAPACITY` |
| Strict OpenSpec validation | PASS | `openspec validate build-v3-live-rocketmq-ordering --strict` |
| Compose, script, source boundary, and diff checks | PASS | Messaging-live Compose resolved; gate script passed `bash -n`; six configuration/source-boundary tests passed; `git diff --check` passed |
| Clean attributable implementation revision | NOT_RUN | No V3 implementation commit was authorized or created in this apply step, so revision-bound reruns remain task 9.7 |
| OpenSpec sync/archive and publication | NOT_RUN | These are separate post-implementation actions and were not requested |

## Retained investigation history

The append-only directories `20260810T115210Z-v3-live` and `20260810T115615Z-v3-live` remain `FAIL`. Their combined-class runs exposed stale Spring test contexts whose distinct RocketMQ consumer groups each received later messages. The fix closes every live test context after its class, shutting down its producers and consumers; the broker outage drill is also ordered last and waits for explicit container state transitions. The final report then passed the same combined selection.

`20260810T113524Z-v3-live` is an earlier passing but narrower checkpoint. The final report supersedes it for current V3 implementation evidence because it contains the expanded acknowledgement-loss, retry-exhaustion, expiration-loss, and comparison matrix.

## Claim limits

This report proves a local disposable topology and the tested failure semantics. It does not establish production availability, failover, persistence, delay SLA, throughput, capacity, or operational readiness. Direct publication still has a prepare-to-Broker crash window; V3 records and reconciles that uncertainty but does not provide V4 Outbox/CDC eventual publication.
